<%= form_with(model: posteditor) do |form| %>
  <% if posteditor.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(posteditor.errors.count, "error") %> prohibited this posteditor from being saved:</h2>

      <ul>
        <% posteditor.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :editor_id, style: "display: block" %>
    <%= form.text_field :editor_id %>
  </div>

  <div>
    <%= form.label :user_id, style: "display: block" %>
    <%= form.text_field :user_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
